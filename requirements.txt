# Core dependencies
pyyaml>=6.0.0
pandas>=2.0.0
lxml>=4.9.0
tabulate>=0.9.0
colorama>=0.4.6
sqlalchemy>=2.0.0

# Oracle (thin mode — no Oracle Client installation required)
oracledb>=2.0.0

# Other database drivers (install only if needed)
psycopg2-binary>=2.9.0      # PostgreSQL
pymysql>=1.1.0               # MySQL / MariaDB
pyodbc>=5.0.0                # SQL Server / any ODBC source
